/**
 * config.js — HP自動生成ツール 設定ファイル
 * ─────────────────────────────────────────────────────
 * セットアップ時に変更が必要な設定をすべてここに集約しています。
 * 非エンジニアのスタッフでも、このファイルだけ編集すれば接続できます。
 *
 * 変更手順：
 *   1. GAS_ENDPOINT に Google Apps Script の Webアプリ デプロイURLを貼る
 *   2. SPREADSHEET_URL に記録用スプレッドシートの共有URLを貼る（オプション）
 *   3. ファイルを保存して完了
 * ─────────────────────────────────────────────────────
 */
var CONFIG = {

  // ===================================================================
  // ① GAS（Google Apps Script）のWebアプリURL
  //    GASプロジェクトを「Webアプリとしてデプロイ」した際に発行されるURLを貼ってください。
  //    例: https://script.google.com/macros/s/ABCDEF.../exec
  //
  //    ※「YOUR_DEPLOYMENT_ID」のままにしておくと「モックモード」で動作します。
  //       モックモードでは実際のAPI呼び出しは行われず、ダミーのURLが返されます。
  // ===================================================================
  GAS_ENDPOINT: 'https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec',

  // ===================================================================
  // ② Cloudflare Pages のプロジェクト名（完了画面のURL生成に使用）
  //    例: 'my-hp-project' の場合、URLは
  //         https://my-hp-project.pages.dev/sites/{siteId}/ になります。
  // ===================================================================
  CLOUDFLARE_PROJECT: 'your-cloudflare-pages-project',

  // ===================================================================
  // ③ 記録用スプレッドシートのURL（完了画面の「スプレッドシートを開く」ボタン）
  //    GASコード側でも SPREADSHEET_ID プロパティを別途設定してください。
  // ===================================================================
  SPREADSHEET_URL: 'https://docs.google.com/spreadsheets/d/YOUR_SPREADSHEET_ID/edit',

  // ===================================================================
  // ④ GitHubリポジトリURL（完了画面の「管理リンク」ボタン用）
  //    例: 'https://github.com/your-org/hp-sites'
  // ===================================================================
  GITHUB_REPO_URL: 'https://github.com/YOUR_ORG/YOUR_REPO',

  // ===================================================================
  // ⑤ 業種定義（追加・変更する場合はこの配列を編集してください）
  //
  //    各オブジェクトのフィールド:
  //      id    : 内部識別子（英字小文字、GitHubのフォルダ名にも使われます）
  //      label : UI上の表示名
  //      icon  : 絵文字アイコン
  //      color : 業種カラー（現在は将来の拡張用）
  // ===================================================================
  industries: [
    {
      id: 'salon',
      label: 'ヘアサロン・美容室',
      icon: '✂️',
      color: '#EC4899'
    },
    {
      id: 'food',
      label: '飲食店・カフェ',
      icon: '🍽️',
      color: '#F59E0B'
    },
    {
      id: 'clinic',
      label: 'クリニック・整体',
      icon: '🏥',
      color: '#06B6D4'
    },
    {
      id: 'school',
      label: 'スクール・教室',
      icon: '📚',
      color: '#8B5CF6'
    }
  ]
};
